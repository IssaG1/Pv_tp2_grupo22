/* declarar variables antes, no olvidar */

let horas = 15;
let minutos = 30;
let horaEnSeg = horas * 3600;
let minEnSeg = minutos * 60;

console.log(`la hora ${horas} en segundos es ${horaEnSeg}`);
console.log(`el minuto ${minutos} en segundos es ${minEnSeg}`);

